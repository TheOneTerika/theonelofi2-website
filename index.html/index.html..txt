<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TheOneLofi2 – Latest Videos</title>
  <style>
    :root {
      --bg-color: #111;
      --text-color: #fff;
      --accent-color: #90caf9;
      --description-color: #ccc;
    }

    [data-theme="light"] {
      --bg-color: #fff;
      --text-color: #111;
      --accent-color: #1976d2;
      --description-color: #444;
    }

    body {
      font-family: Arial, sans-serif;
      background: var(--bg-color);
      color: var(--text-color);
      text-align: center;
      margin: 0;
      padding: 0 1rem;
      transition: background 0.3s, color 0.3s;
    }
    .video {
      margin-bottom: 40px;
    }
    iframe {
      width: 100%;
      max-width: 560px;
      height: 315px;
      border: none;
      border-radius: 12px;
    }
    h1 {
      margin-top: 30px;
      color: var(--accent-color);
    }
    h3 {
      font-weight: normal;
      margin: 10px 0 5px;
    }
    h3 a {
      color: var(--accent-color);
      text-decoration: none;
    }
    h3 a:hover {
      text-decoration: underline;
    }
    .description {
      font-size: 0.9rem;
      color: var(--description-color);
      max-width: 560px;
      margin: 0 auto 15px;
    }
    .toggle-theme {
      margin: 1rem auto;
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 8px;
      background: var(--accent-color);
      color: var(--bg-color);
      cursor: pointer;
      font-size: 1rem;
    }
    .load-more {
      margin: 2rem auto;
      padding: 0.6rem 1.2rem;
      font-size: 1rem;
      background: var(--accent-color);
      color: var(--bg-color);
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1>🎧 Latest from TheOneLofi2</h1><button class="toggle-theme" onclick="toggleTheme()">Toggle Theme</button>
  <div id="video-container">
    Loading videos...
  </div><button class="load-more" onclick="loadMoreVideos()">Load More Videos</button> 
  <script>


    const API_KEY = 'AIzaSyA24xINQ84Qes5kEPp68gxW0wX4gwV4wOs';
    const CHANNEL_ID = 'UCW5vng0jarOyXrRwbaYScCw';
    const MAX_RESULTS = 5;
    let nextPageToken = '';

    function loadVideos(initial = false) {
      let url = https://www.googleapis.com/youtube/v3/search?key=${API_KEY}&channelId=${CHANNEL_ID}&part=snippet,id&order=date&maxResults=${MAX_RESULTS};
      if (!initial && nextPageToken) {
        url += &pageToken=${nextPageToken};
      }

      fetch(url)
        .then(response => response.json())
        .then(data => {
          const container = document.getElementById('video-container');
          if (initial) container.innerHTML = '';

          nextPageToken = data.nextPageToken || '';

          const videos = data.items;

          videos.forEach(video => {
            if (video.id.kind === 'youtube#video') {
              const title = video.snippet.title.replace(/</g, "&lt;").replace(/>/g, "&gt;");
              const description = video.snippet.description.replace(/</g, "&lt;").replace(/>/g, "&gt;");
              const videoId = video.id.videoId;
              const videoElement = document.createElement('div');
              videoElement.className = 'video';
              videoElement.innerHTML = 
                <h3><a href="https://www.youtube.com/watch?v=${videoId}" target="_blank">${title}</a></h3>
                <div class="description">${description.substring(0, 150)}...</div>
                <iframe src="https://www.youtube.com/embed/${videoId}" allowfullscreen></iframe>
              ;
              container.appendChild(videoElement);
            }
          });

          if (container.innerHTML === '') {
            container.innerText = 'No recent videos found.';
          }
        })
        .catch(err => {
          console.error('Error loading videos:', err);
          const container = document.getElementById('video-container');
          container.innerHTML = 
            <p style="color: #f44336;">⚠️ Failed to load videos.<br>Check your API key or network connection.</p>
          ;
        });
    }

    function loadMoreVideos() {
      loadVideos(false);
    }

    function toggleTheme() {
      const html = document.documentElement;
      const currentTheme = html.getAttribute('data-theme');
      html.setAttribute('data-theme', currentTheme === 'light' ? 'dark' : 'light');
    }

    // Initial load
    document.documentElement.setAttribute('data-theme', 'dark');
    loadVideos(true);
  </script>
</body>
</html>